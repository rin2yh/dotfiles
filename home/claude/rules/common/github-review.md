# レビュー運用規約

- レビューコメントに返信し、その指摘への対応が完了（解決）したら、当該レビューコメント／スレッドを「解決済み（resolved）」にする
  - 理由: 未解決スレッドが残ると、対応済みか未対応かをレビュアー側から判別できず、レビュー完了の判断が滞るため。解決済みにすることで、残っている未解決スレッドが「まだ対応が必要なもの」だけになり、状態が一目で分かる
  - 補足: 返信だけで閉じず解決状態まで更新する。解決は MCP（`resolve_review_thread`）で行える
